# Tools

Owns reproducible developer and repository tooling.
# Repository tools

Run `node tools/scripts/doctor.mjs` to inspect the declared development
toolchain without modifying the machine. `pnpm run doctor` is available only
as a convenience after pnpm is already installed. See [development setup](../docs/development/DEVELOPMENT.md)
for supported versions and preparation instructions.
