{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local-only Phase 0.5 desktop shell with no plugin permissions.",
  "local": true,
  "windows": ["main"],
  "permissions": []
}
