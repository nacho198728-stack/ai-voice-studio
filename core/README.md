# Core

Owns shared, cross-language contracts and platform-neutral foundations such as configuration, capabilities, versioning, and telemetry definitions. The `contracts` member is a minimal Rust workspace crate; concrete contracts are deferred.
